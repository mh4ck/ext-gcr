import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gozocarrental.hire',
  appName: 'Gozo Car Rental',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
